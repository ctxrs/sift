{
  "id": "sift-rewrite",
  "name": "Sift",
  "version": "0.1.0",
  "description": "Rewrite supported shell commands through Sift before execution.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": { "type": "boolean", "default": true }
    }
  }
}
